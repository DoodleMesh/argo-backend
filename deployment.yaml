apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: doodlemesh
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      annotations:
        sidecar.istio.io/proxyCPU: "50m"
        sidecar.istio.io/proxyMemory: "64Mi"
        sidecar.istio.io/proxyCPULimit: "100m"
        sidecar.istio.io/proxyMemoryLimit: "128Mi"
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: kush233/draw-backend:8e0f861be3307ce9c55a362d8e1c38f0b9ab7c0a
          imagePullPolicy: Always
          ports:
            - containerPort: 3002
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"
